import setuptools

setuptools.setup(
    name='markovattribution',
    version='0.0.1',
    author='Kailin L',
    description='Fit a Markov attribution model',
    packages=setuptools.find_packages()
)